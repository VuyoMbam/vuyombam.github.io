# Python packages needed to render the site (`quarto render`).
# Quarto itself (>= 1.8) is installed separately: https://quarto.org/docs/get-started/
pandas>=2.2
numpy>=1.26
plotly>=6.0
kaleido==0.2.1    # used by site_utils/export.py to save article preview PNGs. 0.2.1 bundles its own
                  # Chromium; kaleido 1.x needs Chrome installed and failed to start Edge here.
matplotlib>=3.8
jupyter
nbformat
nbclient
ipykernel
pyyaml
